<!DOCTYPE html>
<html>

<head>
<meta charset="utf-8">
<title>Will You Be My Valentine?</title>
<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
<style>
.center-screen {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 100vh;
}
.big-title {
    font-size: 2rem; /* Adjust the size as needed */
    margin-bottom: 20px; /* Space between the title and buttons */
    text-align: center;
}
.button-size {
    transition: font-size 0.5s ease; /* Smooth transition for button resizing */
}
</style>
</head>

<body>
<div id="app" class="center-screen">
    <div class="big-title">Will you be my Valentine?</div>
    <h3>{{ currentHeaderText }}</h3>
    <button type="button" class="btn btn-success mr-2 button-size" :style="{ fontSize: yesButtonSize + 'rem' }" @click="yesButton">Yes</button>
    <button type="button" class="btn btn-danger button-size" :style="{ fontSize: noButtonSize + 'rem' }" @click="changeText">No</button>
</div>

<script src="https://cdn.jsdelivr.net/npm/vue@2.6.14/dist/vue.js"></script>
<script>
new Vue({
    el: '#app',
    data: {
        headerText: [
            "Could you think it over just one more time?",
            "Are you absolutely certain?",
            "Do you want to reconsider your answer?",
            "Wouldn't you like to give it another thought?",
            "Are you sure you don't want to be my Valentine?",
            "Is there no chance you might change your mind?",
            "Are you really, truly sure?",
            "Surely you might consider it again?",
            "Wouldn't you reconsider for me?",
            "Are you positive about your decision?",
            "Couldn't we perhaps talk it over?",
            "Is that your final decision?",
            "Don't you want to think it through once more?",
            "Are you convinced it's the right choice?",
            "Would you mind giving it one more thought?",
            "Are you 100% certain of your choice?",
            "Could I persuade you to reconsider?",
            "Have you considered all possibilities?",
            "Wouldn't you want to ponder it a bit longer?",
            "Are you sure you won't regret this decision?"
        ],
        currentHeaderTextIndex: 0,
        yesButtonSize: 1,
        noButtonSize: 1,
    },
    methods: {
        changeText() {
            this.currentHeaderTextIndex = (this.currentHeaderTextIndex + 1) % this.headerText.length;
            this.yesButtonSize *= 1.2; // Exponentially increase the size of the Yes button
            this.noButtonSize *= 0.8; // Decrease the size of the No button
            // Ensure the No button doesn't disappear entirely
            this.noButtonSize = Math.max(this.noButtonSize, 0.5);
        },
        yesButton() {
            alert('I\'m so happy you said yes!');
        }
    },
    computed: {
        currentHeaderText() {
            return this.currentHeaderTextIndex === 0 ? '' : this.headerText[this.currentHeaderTextIndex];
        }
    },
    mounted() {
        this.currentHeaderText = '';
    }
});
</script>
</body>
</html>
